{
  "name": "gonaturo-railway-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package to let Railway detect Node in monorepo",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
